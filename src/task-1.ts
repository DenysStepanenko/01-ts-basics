// Оголошуємо змінну name з типом string
const name: string = 'Alice';

// Оголошуємо змінну age з типом number
const age: number = 30;

// Оголошуємо змінну isOnline з типом boolean
const isOnline: boolean = true;

// Виводимо значення змінних у консоль
console.log(`Name: ${name}`);
console.log(`Age: ${age}`);
console.log(`Online: ${isOnline}`);
